@AGENTS.md

# SIS Project Context

This is the **Early Childhood SIS** for TK Cita Pelangi school.

**Current state:** Read `sis-app-state-2026-06-16.md` first — it documents every feature's status, the schema, known gaps, resolved decisions, and what's deferred.

**Grading scale:** P / C / B (3 levels only). 0–3 = P, 4–6 = C, 7–10 = B. Do not add a B+ level.

**Deferred — do not build:** Audit/changelog log. Confirmed by Hakiim to be built in a later version once the app is finalized.

**To run the dev server:**
```bash
export NVM_DIR="$HOME/.nvm" && source "$NVM_DIR/nvm.sh" && npm run dev
```

**Demo credentials (password: demo1234):**
- Admin: admin@tkcp.id
- Teacher (homeroom): ms.devina@tkcp.id
- Parent: parent.ayla@gmail.com
